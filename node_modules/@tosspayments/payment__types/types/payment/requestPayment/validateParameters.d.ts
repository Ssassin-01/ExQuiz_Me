import { PaymentRequest, PaymentRequestWithoutUrl } from '../PaymentRequest';
type Parameters = {
    clientKey: string;
    isPromise: boolean;
} & (PaymentRequest | PaymentRequestWithoutUrl);
export declare function validateParameters(params: Parameters): Promise<ValidateResponse['data']>;
interface ValidateResponse {
    data: {
        token: string;
    };
}
export {};
